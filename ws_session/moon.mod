name = "hosi121/ws_session"

version = "0.1.0"

source = "src"

preferred_target = "native"

readme = "README.md"

repository = "https://github.com/Hosi121/servicekit.mbt"

description = "Bounded text WebSocket sessions for moonbitlang/async"

import {
  "moonbitlang/async@0.22.1",
}
